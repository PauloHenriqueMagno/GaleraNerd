import { Switch, Route } from "react-router";

const Routes = () => {
    return (
        <Switch>
            <Route path="/">
            </Route>
        </Switch>
    );
};

export default Routes;